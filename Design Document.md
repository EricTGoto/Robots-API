# Design Document for Robots API

- implement CRUD (create, read, update, delete)
- use a database for persistence
- every robot should have the following:
    - name (string)
    - ownerID (string)
    - identifier (unique string)